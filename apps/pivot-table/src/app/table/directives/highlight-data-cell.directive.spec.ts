import { HighlightDataCellDirective } from './highlight-data-cell.directive';

describe('HighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightDataCellDirective();
    expect(directive).toBeTruthy();
  });
});
