import { HighlightHeaderCellDirective } from './highlight-header-cell.directive';

describe('HighlightHeaderCellDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightHeaderCellDirective();
    expect(directive).toBeTruthy();
  });
});
